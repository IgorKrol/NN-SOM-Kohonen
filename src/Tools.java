public class Tools {
    /**
     * Help function - calculates distance
     */
    public static double EuclideanDistance(double x, double y){
        return  Math.sqrt(Math.pow(x,2)+Math.pow(y,2));
    }
}
